Implement the SOLWEIG-light optimization using this repository's `optimization_v5_claude/` packet. This is a Claude Code task, not a Codex/Astra task. Read applicable project instructions, then DESIGN_AUTHORITY.md, PLAN.md, MODEL_ROUTING.md, VALIDATION_POLICY.md and TASKS_CLAUDE.yaml. Follow CLAUDE_CODE_EXECUTION_PROMPT.md and the relevant IMPLEMENTATION_BLUEPRINT.md recipe. The high-level design is already supplied; implement, inspect, test, repair, independently review and integrate it rather than redesigning everything.

Use the working GLM-5.3 profile as coordinator. Delegate difficult numerical implementation and independent proof/code review to actual Anthropic Opus when genuinely connected. No required Astra/OpenAI calls. `model: opus` under a Z.ai-remapped alias is still GLM, not Opus. Use separately authenticated Claude Code profiles/processes unless an existing gateway verifiably routes both providers. Record actual routing; never log/copy credentials or invent account access. Missing Opus is recorded honestly and does not stop unrelated work.

Create a separate local `perf/claude-glm53-cpu-v5` worktree/branch from actual intended HEAD. Preserve user edits and prior optimization work. Verify every worker's exact base; native worktrees may otherwise start from the default branch. No main changes, automatic push/PR/merge/release. Historical optimization checkpoint: 14e888760727583ef782a4dc0e7a5c7c6e6ff9d1. Model/API ancestry: 0d7fe742abeeddd890dd58fc76ed7f78bd47faec.

There is no artificial token budget, four-agent cap or total-agent cap. Parallelize useful dependency-ready disjoint tasks to actual provider/client capacity. One writer per scope; one integrator owns shared engine/pipeline/API/ledger. Keep local compilation/test CPU/RAM centrally bounded, and benchmarks exclusive. Unlimited inference does not mean unlimited machine jobs or large tests. Use completion notifications or blocking waits, not progress polling or transcript replay. Do not ask for intermediate approval inside authorized local scope.

Preserve all seven workflows, public/CLI/artifact semantics, current physical model, typed math profile, chronological state, original ray/patch sequence, first-step/bush/boundary/water behavior, guards/fallbacks and durability. Torch/CUDA-free own-met core. No approximations, reduced physical work, tolerance relaxation, candidate-made goldens or fake passes. Prioritize guarded sky absorption/suffixes, ordered radiation decode/SIMD and exact partial evaluation, GVF ownership/fusion, and liveness-based memory/persistent workers. Use the catalog selectively, not all 56 ideas.

Keep v4's lean validation: L0 tiny proofs; L1 actual kernels mostly 16-128 square; L2 real 64-128-square TIFF with full 24/48-step chronology and 153 patches; central L3 128/256-square shortlist. Reuse dependency-identical evidence, environments and stable wheel builds. No blanket suite after every edit and no worker-level 1024 runs.

Freeze the combined source, wheel, environment, actual data and primary cache/output/checkpoint/resource protocol, then run the actual 24 x 1024-square batch once at the end. Target <=1800 s through required publication. New large baseline defaults to zero; at most one justified missing-reference capture. At most one causally fixed final retry. Preserve failures and report unverified scope; one pass is not statistical reliability or complete P7/P8 release qualification.

Continue until the best verified branch and the allowed final campaign are delivered or genuinely blocked. Do not stop at a prototype/import pass. Use local Opus review for ordinary proof questions, not a return to Astra. For an actual scientific/authorization boundary record one compact exception and continue other tasks. Finish with branch/base/tip, reviewed patches, honest tests and measurements, deferred gates and reproducible merge-review notes. Leave the branch unmerged.
