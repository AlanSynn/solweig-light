# SOLWEIG-light optimization v5 project rules

The user has moved implementation to Claude Code. Use GLM-5.3 coordination and actual Opus implementation/review where connected. No required Codex/Astra/OpenAI calls. This v5 direction supersedes older agent-tier assignments and artificial four-agent/token caps, not numerical/security invariants. Read the assigned packet plus only necessary docs.

Design authority: `optimization_v5_claude/DESIGN_AUTHORITY.md`.
Implementation recipes: `optimization_v5_claude/IMPLEMENTATION_BLUEPRINT.md`.
Operational plan/DAG: `optimization_v5_claude/PLAN.md`, `TASKS_CLAUDE.yaml`.
Routing: `optimization_v5_claude/MODEL_ROUTING.md`.
Validation/branch: `optimization_v5_claude/VALIDATION_POLICY.md`, `BRANCH_AND_CI.md`.
Paths above are navigation, not invitations to load every document each edit.

- Preserve all seven workflows, exact required API/CLI/artifacts, Torch-free own-met core, typed numerical profile, chronological state, logical domains and durability. No approximation, tolerance relaxation, fake golden or hidden skip.
- Work on the dedicated local optimization branch/worktrees. No automatic push/PR/main merge/release; no stash/reset/discard of user changes. Verify each worker's base explicitly.
- `opus` can be an alias remapped to GLM. A genuine Opus claim requires a verified provider route. Secrets stay outside repository, prompts and logs. Do not modify global authentication automatically.
- No artificial limit on useful inference sessions or total tokens. Dependencies, provider/client limits, exclusive file ownership, CPU/RAM/IO admission and benchmark isolation still constrain execution.
- One integrator owns shared engine/pipeline/API/schema/central ledger. Worker alternatives and tests have separate write scopes. Reviewer is independent of implementer.
- Workers complete implement/test/inspect/repair without routine approval. Use terminal events or blocking waits, not LLM status polling. Policy-level blockers are recorded compactly; independent work continues.
- L0/L1 tiny/actual kernels; L2 real small TIFF with 24/48 steps and 153 patches. L3 is a central small lab, L4 final-only once. No ordinary worker 1024 tests. Preserve v4's one corrective large retry and missing-reference limits.
- Run tests on coherent behavior changes and changed dependencies. Reuse exact valid evidence. Do not rebuild/retest for status edits or reopen full old logs every turn.
- Performance runs have exclusive host ownership. Other agents do not run local builds/tests/profilers during the timed interval.
- Proof sketches and inherited synthetic checks are not runtime verification. Report actual route/source/commands/results and unavailable scope honestly. End with an unmerged branch and merge-review packet, not claimed full release completion.
