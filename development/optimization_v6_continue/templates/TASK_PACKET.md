# Immutable worker packet
Task ID / assigned role / actual served model record:
Exact base SHA / detached worktree / protected paths:
Owned files / integrator-only touchpoints:
Read only these dossier sections and code ranges:
Expected private interface:
Proof: domain, typed operations, order, aliases, masks, errors, warnings, ownership:
Reference source/data hashes:
Permitted test tier and shared CPU/RAM/disk reservation:
Terminal outputs: scoped diff/commit, proof, commands/exit codes, comparisons, measured class, failures, next dependency:
Reject/stop rules; no new named branch, no large test, no routine Astra call:
