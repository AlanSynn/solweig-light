Continue implementation on perf/native-optimization using optimization_v8_native_default/. As GLM-5.3 coordinator, delegate bounded implementation, proofs and independent review to authenticated Opus/GLM agents. Implement, test, repair and integrate.

Read applicable root instructions, this packet's DESIGN_AUTHORITY.md, DX_CONTRACT.md, PLAN.md, VALIDATION_POLICY.md and TASKS_CLAUDE.yaml once. Preserve the current HEAD and user changes. Known native baseline: 16cdc56cbc8755675487b9c9c1f5a7e987e2c9e4; main DX reference: 14e888760727583ef782a4dc0e7a5c7c6e6ff9d1. Never reset to these pins. Stay on the same named branch. Detached worktrees at explicit commits are allowed. Only the integrator commits shared code. No push, PR, main merge, release, credentials edits or automatic hosted CI.

Goal: native execution must become a worthwhile automatic default in qualified environments, with unchanged main-style installation, seven public workflows, imports, CLI, argument defaults, TIFF schemas, chronology and durability. Users must not need a backend flag, native extra, ISPC/compiler/zsh installation, runtime compilation/download. Ship prebuilt native artifacts inside the same distribution's platform wheels. Keep compiler-free source installs and unsupported platforms working through Numba. Existing GDAL prerequisites remain; source fallback success is not native deployment success.

First measure actual packed-path residuals and worker-native calls/fallbacks. B7-60 native lost end-to-end. The 1.5-2.2% island fraction was a synthetic dense fallback, not the real packed pipeline. Build stamps are not call counts; parent RSS is not process-tree RSS.

Implement narrow changes: (1) pin native handles once per worker/workflow; remove repeated mkdir/stat/JSON/kernel hashing from every block, without losing ABI/ISA/source identity; (2) emit AoSoA directly from accepted visibility/classification producers and compare current Numba A, equivalent-layout Numba B and native C; (3) decouple cache-sized microblocks from coarse dispatch regions and use one bounded multicore owner; (4) only where measured useful, exact longwave predicate codes, coefficient reuse and stored-export single-pass validation. Keep failed fusion/prepared-decoder routes off unless a genuinely changed design earns new evidence.

Preserve both ordered patch sweeps, actual float64 surface chains, explicit float32 rounding nodes, original SLEEF, raw visibility and error/alias semantics. Never round f64 contributions before the accumulator add. No tree reductions, model cuts, relaxed math or diagnostic zeros. Native failure after launch is an error, not hidden fallback. Unsupported inputs and unqualified hardware decline before launch.

Freeze evaluation workloads, fixed CPU/RAM budgets and promotion gates before new results. Default block=128 and production block=1024 both matter. Compare installed no-env auto against current default and strongest equivalent-layout Numba, including preparation, conversion, synchronization, I/O, actual threads and entire process-tree memory. Native must really execute. Do not default-enable based on a microbenchmark or a fallback-only result.

Develop on tiny kernels and real 64/128-square TIFFs with 24/48 steps; use 128/256-square batches for selection. Final 1024/24-spatial-tile work only after source/wheel freeze, one candidate campaign, at most one causal retry. Missing corpus/reference/platform is unverified, not passed. No broad repeated CI or benchmark sweeps.

Parallel reasoning has no artificial total-agent/token cap; local CPU/RAM/storage remain bounded and the benchmark owner is exclusive. Use completion events, not status polling or transcript replay. Reviewers differ from authors. Finish with same-branch reviewed commits, locally tested wheel/source paths, explicit default eligibility and rollout evidence, raw trials and rejections. If native cannot beat Numba end-to-end, retain improvements but leave native promotion incomplete.
