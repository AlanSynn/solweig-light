{
  "schema": "sw8-n8-30-review-v1",
  "review_of": "N8-40b public LW route seam diff on top of HEAD 730eb4de (author n8-seam; this review independent, reviewer did not author)",
  "reviewer": "n8-rev-n8-41 (independent; previously reviewed N8-41 vendoring, n841-3 install_assets, n8-43 freeze -- not this diff)",
  "review_utc": "2026-09-23T03:44Z",
  "worktree": "/Users/alansynn/Workspace/solweig-v8-native @ HEAD 730eb4debab06992eaed83bc90415d51599caf9e, branch perf/native-optimization",
  "working_tree_at_review_start": "VERIFIED exactly the four declared paths: M src/solweig_light/radiation/engine.py, M src/solweig_light/radiation/cylinder_longwave.py, ?? tests/optimization_v8/integration/test_lw_public_seam.py, ?? optimization_v8_native_default/evidence/integration/n8_40b_public_seam_record.json. The reported incident (exploratory repo-wide pytest clobbering optimization_v6_continue raws) is fully cleaned: no v6 path appears in git status. NOTE: my own verification runs re-dirtied installed_gates.json (see N2) -- the deliberate footprint at review end is still exactly the four paths plus that disclosed run-metadata class.",
  "verdict": "APPROVE",
  "blocking_defects": [],
  "required_repairs": [],
  "recommended_repairs": [],
  "claims": [
    {
      "claim": "1. Mechanism: engine.py:1663 passes parallel=True if threads_per_worker>1 else None -- the ONLY production call site of the LW drivers; route gate 'if parallel is not False and rows*cols:' makes explicit False the sole serial demand, None route-eligible",
      "status": "PASS",
      "finding": "Diff read in full. engine.py:1663 (inside Solweig_2022a_calc, under the PIPELINE_CYLINDERS_ANISOTROPIC demand scope): 'parallel=True if get_runtime_options().threads_per_worker > 1 else None' with the 7-line N8-40b comment block. Grep census over src/: Lcyl_v2022a_by_demand has exactly one src caller (engine.py:1663; import at :1653); cylinder_longwave-internal chain Lcyl_v2022a_by_demand:475 -> Lcyl_v2022a_primary:453 -> define_patch_characteristics_primary. The route gate now sits at cylinder_longwave.py:396 'if parallel is not False and rows*cols:' with the single consult _lw_region_route at :397 (def at :181); _lw_region_route keeps the package's only env-read standdown (native/ispc -> None) before _lw_dispatch.region_route -> resolve_lw_backend. Site-inventory cross-check: engine wrappers at :1762 (Kside shortwave), :1770 (define_patch_characteristics), :1778 (Lcyl_v2022a) delegate to patch_radiation drivers with 'parallel=threads_per_worker > 1' (boolean, unchanged); grep proves patch_radiation.py never imports cylinder_longwave or _lw_region_route -- those sites cannot reach the route under any H. EXECUTED: test_public_wrapper_h_value_is_no_serial_demand_at_h1 captures the kwarg at the exact seam (monkeypatched Lcyl_v2022a_by_demand raising _SeamStop) and asserts [None] at H=1 and [True] at H=2 -- passed in my integration run."
    },
    {
      "claim": "2. NO None reaches njit: every falsy-parallel branch resolves exactly as False; FULL_DIAGNOSTICS bool(parallel) is behavior-identical for every existing caller",
      "status": "PASS",
      "finding": "Code-verified at the current tree. (a) _lw_kernel (cylinder_longwave.py:203, body :215): 'kernel=_longwave_primary if parallel else _longwave_primary_serial' -- truthiness, None -> serial. (b) _longwave_fused_primary_block (:332) is PLAIN PYTHON (no decorator); its kernel selection is the same truthiness pattern; the njit fused pair (_longwave_fused_primary :226-227, _longwave_fused_primary_serial :279-280) have signatures ending at reflection_factor -- no parallel parameter exists to receive None. (c) the non-fused fallback call 'kernel(sh,vs,vb,...,factor)' passes no parallel. (d) FULL_DIAGNOSTICS branch (:472-473) now 'parallel=bool(parallel)' so patch_radiation keeps its boolean domain: bool(None)=bool(False)=False, and every existing caller passes True/False (engine previously passed an H-derived bool; v6 parity tests parametrize parallel over (False, True) only -- grep-verified; by_demand calls in tests rely on the unchanged default parallel=True). The only None producer in the tree is the new engine.py:1663 expression."
    },
    {
      "claim": "3. Shipped-state inertness EXECUTED: public H=1 call bitwise-identical to route-forced-None, exactly ONE resolve_lw_backend call, zero pools, serial kernels only; H=2 identical to today",
      "status": "PASS",
      "finding": "EXECUTED in my integration run (14 passed). test_shipped_h1_public_call_consults_once_and_stays_serial drives the REAL public calc (engine.Solweig_2022a_calc on small_original_cpu input event timestep 7 under cyl.demand_scope(PIPELINE_CYLINDERS_ANISOTROPIC)) at threads_per_worker=1 with the real shipped registry and asserts: len(consults)==1 on a recording delegator around the real policy.resolve_lw_backend; '[absent]' present in policy_reasons (real fail-closed decision, not a stub); region_spy == [] (execute_regions never called); _longwave_primary and _longwave_fused_primary counts == 0 with serial family >= 1 (per-kernel monkeypatch delegators on all four cylinder njit kernels -- module-global call sites make the wrappers visible); Ldown/Lside bitwise (uint32 views) vs the same call with _lw_region_route forced None. test_shipped_h2_public_call_identical_to_today: H=2 bitwise vs forced-None, exactly 1 consult in the routed run, parallel family taken / serial family zero. Record's standalone probe (H1 and H2: resolve calls 1, live pools 0) is consistent."
    },
    {
      "claim": "4. Qualified state: injected B-row through the PUBLIC wrapper at H=1 dispatches the region path and stays bitwise; driver-level parallel=False with a QUALIFIED record never dispatches; no existing test modified",
      "status": "PASS",
      "finding": "EXECUTED. test_qualified_row_h1_public_call_dispatches_region injects a qualified row B record (python-module kind backed by the real lw_b_control.py sha256, fabricated promotion/review tree with tools/ mirrored for _assess_promotion) and drives the PUBLIC calc at H=1: exactly one execute_regions (self_parallel consumer, report.blocks == plan.total_blocks), ALL four numba kernel counts zero, policy.resolve_lw_backend().row == 'B', then bitwise vs the route-forced-None legacy run. test_driver_explicit_serial_demand_never_dispatches_qualified: under the injected qualified row, driver-level parallel=False produces zero consults and zero dispatches while parallel=None dispatches, bitwise-equal outputs -- False alone is the serial demand. Existing tests untouched: git status shows no modified test file; test_serial_demand_never_dispatches (test_lw_dispatch.py) passes unchanged inside the 14."
    },
    {
      "claim": "5. New test file quality: no bare conftest imports, real reference-scene anchor, skip-label discipline",
      "status": "PASS",
      "finding": "tests/optimization_v8/integration/test_lw_public_seam.py: the v6 conftest is loaded by importlib spec_from_file_location under the UNIQUE module name '_n840b_v6_conftest' (no shadowing); fabricated-evidence builders come from 'policy_test_helpers' (unique name, not conftest). Anchor is REAL: _seam_load_inputs reads tests/reference/small_original_cpu/boundaries/manifest.json, selects the boundary=='input' event at timestep 7, packs visibility via PackedVisibility.from_dense and diffsh via LazyDiffVisibility exactly as the pipeline profile. Day-branch provenance is enforced by construction: the seam-capture test asserts captured == [expected]; a night event would never reach Lcyl_v2022a_by_demand and the test would fail on captured == []. Skip labels: the module docstring declares 'none -- row B is a python-module row and needs no staged native artifact' and the file contains no pytest.skip -- correct, and the run confirms zero skips in integration. Autouse fixture restores shipped env and tears down pools + policy state per test."
    },
    {
      "claim": "6. Suites: integration 14, policy+dx 95, installed 13/6, full tree 1972/6/0",
      "status": "PASS",
      "finding": "ALL EXECUTED BY ME at HEAD 730eb4de with the diff applied (.venv/bin/python -m pytest -q -p no:cacheprovider; 1-min loadavg before each run: 4.84 / 4.42 / 5.09 / 8.05 -- all >= 2.5, no stop fired; full tree started 23:36 local, away from quiet-window marks). integration: 14 passed in 5.48s (9 pre-existing N8-40 + 5 new seam tests, 0 skipped). policy+dx: 95 passed in 3.28s. installed (dedicated): 13 passed / 6 skipped in 152.33s, skip decomposition verified by -rs: 4 deferred gates ([native-wheel-deferred-N8-41], [companion-collision-deferred-N8-41], [upstream-collision-deferred-N8-42], [native-qualification-deferred-N8-42]) + 2 [host-memory-pressure] -- unchanged from prior heads. FULL TREE tests/optimization_v8/ (one invocation): 1972 passed / 6 skipped / 0 failed in 200.58s = baseline 1967 + exactly the 5 new tests; same 6-skip decomposition; same 4 pre-existing warnings (1 native cast RuntimeWarning + 3 region fork DeprecationWarnings)."
    },
    {
      "claim": "7. Record: design-first with rejected candidates, shipped-state delta stated explicitly, site-inventory correction documented",
      "status": "PASS",
      "finding": "n8_40b_public_seam_record.json contains: design.rejected_candidates with THREE mechanism-level rejections (unconditional True -- violates shipped-state inertness; second policy-shaped flag -- wider driver surface, forkable inconsistency; wrapper-level consult -- second consult path violating the single-env-read constraint), chosen_mechanism, new_parameters 'None', serial_demand_preservation, qualified_state_behavior, shipped_state_delta_statement ('exactly one resolve_lw_backend() registry read per public LW driver call' as the ONLY H=1 delta; h_over_1_delta 'None'), and site_verification.brief_correction documenting that engine :1770/:1778 delegate to patch_radiation drivers that cannot reach the route -- independently confirmed by my grep (patch_radiation never imports cylinder_longwave; the :1770/:1778 wrappers call .patch_radiation compiled drivers with boolean parallel) and by the record's own reasoning that rerouting them would drop the four cardinal outputs. The record also discloses the working-tree regeneration class (working_tree_note_for_integrator) and a negative control (git checkout -- pre-seam sources: 4 fail / 1 pass)."
    }
  ],
  "negative_control_corroboration": "The author's destructive checkout probe was NOT re-executed (it would destroy the diff under review); corroborated structurally instead: pre-seam, the H=1 capture would read False not None (test 1 fails), the shipped H=1 gate would stay shut so consults==0 not 1 (test 2 fails), None-as-False would leave region_spy empty under the injected qualified row (tests 4 and 5 fail), and only the H=2 no-regression test passes on both trees -- exactly the recorded 4 fail / 1 pass split. Non-vacuity holds: no new test can pass on the pre-seam sources except the H=2 one that is supposed to.",
  "notes": [
    "N1 (presentational): the record cites PRE-DIFF line numbers for the changed sites -- engine ':1655'/'1653-1655', gate ':387', consult ':388' -- which are the positions at head_at_start 3079d69a; post-diff they are engine.py:1663 (call), cylinder_longwave.py:396 (gate), :397 (consult). The untouched-site citations (:1762/:1770/:1778, region/consumers.py:33) are current. Reconstructable from head_at_start; no repair required.",
    "N2 (tree hygiene, action for coordinator/integrator): my verification runs regenerated optimization_v8_native_default/evidence/installed/installed_gates.json (67/67 changed lines -- pytest tmp-dir indices pytest-863 -> pytest-876 and host memory-budget figures inside captured ResourceAdmissionError strings; outcomes identical). This is exactly the run-metadata class the record's working_tree_note_for_integrator discloses; restore with 'git checkout --' if the regenerated numbers are not wanted. I did not restore it (guardrail: writes only under evidence/reviews/ and /tmp). The v6 raws were NOT touched by my scoped runs.",
    "N3: the record's probe evidence and suite ladder were taken as recorded (not re-executed by me); every load-bearing number they contain that is also a test assertion (consult-once, bitwise, pool-zero, 14/95/13-6/1972-6-0) was independently reproduced in my own runs.",
    "N4 (forward-looking, no action): the tri-state makes None a route-eligible value on the public driver signature; any FUTURE caller passing parallel=None gets one selector consult plus serial kernels. Today the only None producer is engine.py:1663 and the docstrings document the contract on all three drivers; nothing to change."
  ],
  "conclusion": "The seam rework does exactly what it claims and nothing more: H>1 byte-identical, H=1 gains exactly one fail-closed registry read over an otherwise bitwise-identical serial path, explicit False remains the only serial demand, no None reaches any njit kernel, and a qualified row dispatches through the public wrapper at the shipped default. All 7 claims PASS with executed evidence; verdict APPROVE."
}
