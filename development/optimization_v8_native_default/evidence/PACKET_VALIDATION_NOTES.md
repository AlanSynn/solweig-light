# Packet validation only

The first helper-suite run had one failure: START_HERE.txt was 4,017 characters, above the optional 4,000-character startup budget. The text was shortened without removing product/numerical contracts. The final run passed all 52 tests with no failures, errors or skips. Both initial and final logs are retained.

Tests exercise arithmetic models, structured-policy gates, source AST snapshots, static fake-wheel audits, temporary dummy Git repositories, safe setup tools and document/DAG consistency. They do NOT execute SOLWEIG, an upstream oracle, a real native kernel, a deployable wheel, a GPU, Claude Code or a GLM/Opus agent. No application speedup or native default promotion has been measured here.
