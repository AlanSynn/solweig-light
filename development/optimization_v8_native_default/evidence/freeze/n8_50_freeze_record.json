{
  "record": "N8-50 FREEZE: final source/wheel state and scale protocol of the retained (non-promoted) state",
  "recorded_utc": "SET_AT_COMMIT",
  "frozen_state": {
    "branch": "perf/native-optimization",
    "closure_commit": "b4a4c5ba (N8-40 formal completion + N8-42/43/44 unavailable-state closure; N1/N2 docstring repairs)",
    "freeze_delta_note": "this freeze adds: the installed-suite conftest repair below, the regenerated freeze-time installed_gates.json, and this record; no src or shipped-policy change",
    "selection": "numba_improvement_only_native_goal_open (commit 4761c4d6)",
    "shipped_state": "row A (main-parity Numba) everywhere by fail-closed empty registry; vendored machinery inert; candidate native wheel ships staged generation with no qualification claim"
  },
  "locally_tested_paths": {
    "policy+dx suites": "95 passed (env-var surface equals frozen dx baseline with the one self-validating vendored-driver exclusion; registry empty; template pending+inert; legacy env parity byte-compat)",
    "integration+region suites": "167 passed in 41.6s",
    "installed wheel/source suite": "24 passed, 4 skipped in 95.6s -- FIRST COMPLETE PASS of both no-env pipeline gates: offline wheelhouse -> plain pip wheel -> fresh venv outside the repo, API and real CLI workflow under read-only site-packages, network guard, non-writable native cache (auto never builds, Numba JIT cache proves the fallback executed), full 48-band UTCI+TMRT default output contract verified via GDAL",
    "defect_found_and_repaired_at_freeze": "installed_cli_run fixture never set record['scene'] (the API fixture did); test_noenv_cli_real_invocation reached the output assertion for the FIRST time because prior runs were always [host-memory-pressure]-skipped -- KeyError exposed the latent fixture bug; repaired (mirror of the API fixture) and the suite re-run green. The gate itself had passed on the first attempt; only the evidence-record shape was defective.",
    "freeze_time_gates_snapshot": "evidence/installed/installed_gates.json (regenerated by the freeze-time run; api_gate=passed, cli_gate=passed, 4 labelled deferred skips)",
    "skips": "4 = the deferred native-channel labels (native-wheel/companion/upstream/qualification), dispositioned by N8-42's unavailable-state record and the selection"
  },
  "dx_surface_recheck": {
    "method": "test_src_env_var_surface_equals_frozen_dx_baseline (green in the 95): re-derives the src env-read surface and compares to the frozen branch_surface.json; the one delta remains the vendored N8-20 build driver, whose entry is self-validating (must read exactly ['PATH', 'SOLWEIG_LIGHT_ISPC'], build-time only)",
    "conclusion": "branch surface AFFIRMED at freeze, not re-frozen: no new runtime env var, no CLI/import surface change since the N8-01 baseline"
  },
  "scale_protocol_N8_51": {
    "status": "UNAVAILABLE STATE -- no final scale campaign",
    "reason": "N8-51's final 1024/24-spatial-tile campaign exists to qualify a native DEFAULT candidate at scale; the selection advanced no candidate (numba_improvement_only_native_goal_open). Running a scale campaign without a candidate would produce a record with nothing to promote. Per the goal contract: source fallback success is not native deployment success, and missing-candidate is unverified, not passed.",
    "what_was_measured_instead": "the production block (B=1024) WAS measured end-to-end in tier B under the amended quiet window (3 mixes x REPS=9, parity, min-of-9, both accountings) -- the block-level production evidence exists in n8_31_tierB_record.json; the whole-scene driver-level campaign remains unwritten because the flipped-design native path it would measure does not exist",
    "reactivation": "any future campaign reruns the frozen N8-42/43 cells and this protocol after new pre-registered evidence per the selection's flip conditions; no gate loosening after failures"
  },
  "yaml_gate_disposition": {
    "actual_target_availability_and_references_checked": "checked and recorded honestly: native target NOT advancing (selection); references = frozen dx baseline (affirmed), N8-31 tier records, N8-41 wheel draft, installed gates snapshot",
    "one_final_workload_campaign_budget": "consumed by tier B (one candidate campaign, zero retries needed -- it completed censored-free); the at-most-one-causal-retry budget remains unspent"
  },
  "next": "N8-60 independent same-branch handover (yaml role: independent_reviewer)"
}
