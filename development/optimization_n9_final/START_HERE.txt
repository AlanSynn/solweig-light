Complete the FINAL optimization decision and prepare a merge-ready SOLWEIG-light tree. Work on the existing perf/native-optimization branch, not main or a new integration branch. Reviewed checkpoint: 7abe526aae2f97e689b1a1e4ae183e5370f29aa1; reviewed main: 14e888760727583ef782a4dc0e7a5c7c6e6ff9d1. Record current local/remote heads; never reset newer or dirty work.

Read this packet's execution prompt, PLAN.md, LAST_ATTEMPT.md, MEASUREMENT.md and MERGE_SCOPE.md. Consult existing project safety/numerical instructions, but this finalization mandate supersedes old instructions to leave native perpetually OPEN or to implement the full research catalog. Preserve old records, gates and failures. No Astra/Codex invocation is required. Delegate bounded implementation/proof/review to authenticated GLM/Opus routes; names alone do not prove provider identity. No artificial inference-token cap, no repeated status polling, no recursive agent proliferation. One owner per writable scope and one exclusive benchmark owner.

N8 did NOT achieve default native or deploy B: its selection explicitly leaves both off. Its B=1024 timings are sums of component minima, not a continuous production-pipeline run. Its dormant dispatcher expands whole-scene visibility and masks before executing regions. Fix these issues in one bounded final candidate, not a new framework search.

Candidate scope only:
1. Mode-specialize exact binary/ternary decode (constant shifts/masks), preserving raw bits, original patch/pixel/error order, tails and mapped lifetime. Give equivalent optimizations to the Numba control.
2. Produce AoSoA masks directly with the existing exact classifier; measure its full cost. Clear valid inactive lanes on scratch reuse. Do not simply subtract pack_masks from an old record.
3. Prepare immutable coefficient/artifact/ABI validation once per invocation; retain cheap block bounds and ownership checks. Use borrowed descriptors and bounded block/region buffers, not full N*P materialization. Keep the existing kernels and exact mixed-f32/f64 arithmetic.

Implement one candidate family, then one independent review/repair pass. Run the actual packed producer->classifier->consumer->output wrapper continuously, compare frozen A, strongest new Numba B, and native C at B=128/1024, binary/mixed/raw, actual f64 provenance and matched threads. Preserve N8 quantitative promotion gates; run a small real 24/48-step TIFF pipeline, no-env installed API/CLI and native-entry coverage before any default promotion. The bundled Linux decoder probe is synthetic, not M1 or application evidence.

No new GPU/framework/precision/math-model project, no unbounded tuning grid, no further architecture restart. Start/end resource snapshots and bounded quiet-window retries replace hours of loadavg polling. A performance loss closes native for this release. An environment block without credible qualification also closes native for this release; preserve the blocked evidence.

In parallel, audit the full main-to-candidate diff and prepare the CPU-only merge scope. Keep accepted numerical, cache, chronological and resource improvements with real evidence. On native failure, remove dormant N8 production dispatcher/qualification machinery from the shipped path, preserve research and failures outside installed runtime, and retain established explicit expert compatibility. Do not claim B improvements are deployed merely because files exist.

Finish with exactly one selection: closed_native_qualified, closed_cpu_only, or blocked_no_safe_merge. Emit FINAL_SELECTION.json and MERGE_MANIFEST.json containing immutable source/main/tree hashes, active default call graph, measured vs hypothetical claims, exact included/excluded scopes, commands and remaining release limitations. No native_goal_open handoff. Do not weaken correctness to force a merge. Prepare/rehearse the merge locally; do not push, open PR, merge main, release or delete remote history without separate authorization.
