# Execute a final, bounded optimization decision

You coordinate in Claude Code using the user's configured GLM route, with independently authenticated Opus/GLM specialists when available. Design decisions are supplied here. Execute implementation, relevant tests, repairs, independent review, release-scope cleanup and final immutable manifests. Do not return another research plan or require Astra/Codex.

## Authority and continuation

Stay on `perf/native-optimization`. Confirm current HEAD and main, dirty paths, worktree ownership, native artifacts and dependency state. The review checkpoint is 7abe526aae2f97e689b1a1e4ae183e5370f29aa1, main 14e888760727583ef782a4dc0e7a5c7c6e6ff9d1. Never reset/stash/delete unrelated work. Detached worker worktrees may isolate changes at explicit SHAs; only the integrator commits the shared branch. No force push, new integration branch, automatic remote write, merge to main, credentials change or release. Final merge authorization is separate.

This is the LAST native optimization campaign for this release. Preserve past evidence unchanged, but replace the open-ended completion policy with DECISION_CONTRACT.md. Ordinary implementation bugs can be repaired locally; failed measured performance is not permission to invent another candidate family or alter a gate. Required unresolved correctness/installation defects prevent merging the affected change.

## Product contract

Keep all seven APIs, Python argument/default semantics, CLI, companion distribution ownership, TIFF schemas/metadata, numerical profile, chronological state, checkpoint durability and resource budgets. No lower resolution, fewer patches/timesteps, shortened physical support, reduced radiation or implicit fast math. Default use remains compiler-free for wheels and requires no env changes. Preserve the established explicit B7 expert route unless a reviewed, complete replacement satisfies its contract. Default native is a performance-qualified implementation choice, not a forced device choice.

## What the old record actually means

N8 B1 and C1 both lost the production-block accounting vs A0p, and neither B nor C was enabled. The record consists of separately timed component minima, not a continuously timed full wrapper. `_lw_dispatch._execute_row` materializes whole-scene arrays despite the final consumer using regions. The native leaf is fast, but integration cost and residency dominate. Do not call file retention deployment or convert N8's outcome label into a performance claim.

## One final candidate family

1. Specialize the packed producer on known modes 1/2/4 with compile-time shifts and masks. Preserve accepted raw schedule, exact uint32 bits, zero/NaN payloads, first reserved-code failure order, shape/bounds and tail handling. Apply the same eligible improvement to the strongest Numba control. Inspect generated target code; the bundled Linux probe is a lead, not qualification.
2. Use the existing exact direct AoSoA classifier rather than dense mask generation plus pack. Charge actual classifier work and required clearing. `sun_out`/`shade_out` reuse must initialize valid inactive columns; `shade = not sun` is invalid at equality/NaN.
3. Prepare coefficient, artifact, scalar-provenance and ownership guards at their correct lifetimes. Build private unforgeable-by-public-API plans. Keep cheap block bounds and writeability checks. Retain public defensive adapters. Never remove an input-integrity boundary without replacement.
4. Replace whole-scene AoSoA allocation with bounded producer/consumer slots. Preserve the exact two reduction sweeps. Initially keep the existing native kernel. Use existing bounded scheduling only; no new execution framework. Produce/classify/consume inside one actual stream measured from accepted packed inputs to owned outputs.

Do not open GPU, Dr.Jit, PoCL, new geometry, surrogate, precision or dynamic-learning projects. A release-acceptable Numba improvement is valuable; it does not constitute native success. Choose between at most two closely related producer layouts on tuning data, then freeze one integrated candidate. Keep rejected data.

## Fair evidence, not component arithmetic as a verdict

A_main is the current main product, A8 the accepted current default, B the best identical producer/layout/schedule in Numba, C the native consumer. Use A8/B/C for native attribution and A_main/final for what users gain. Freeze identical stage boundaries, scalar f64 provenance, B=128/1024, binary/mixed/raw, native masks and CPU/RAM budgets. Reuse recorded baselines only with matching dependency/harness identity.

Continuous timing includes actual decode, classification, allocations/clears, guards, task submission, native invocation, completion, output placement and once-per-workflow setup. Component timers are diagnostic and nested values are not summed twice. Actual native entry/completion counters and pixel counts prove execution; library-load/build stamps do not.

Keep N8 promotion gates (1.10 geometric-mean installed improvement over A0, 1.05 per eligible cell over A0 and strongest B, protected regressions <=3%, actual native coverage, numerical/DX/memory obligations). N9 uses corrected continuous timing, not an advantageous redefinition of N8's old measurements. A new protocol is frozen before this candidate's target-host results. No retrospective gate weakening or dropping raw/production/thread cells.

## Bounded execution and independent review

Read scope-specific documents, not every historic transcript. Delegate three coherent scopes: producer/Numba control, stream/native plan, measurement/release cleanup. Additional proof reviewers may reason independently. One owner per shared file, one integrator, one benchmark lease. Completion events replace polling. Do not keep an expensive model awake waiting for a quiet host.

Use L0/L1 tests, actual 64/128-square 24/48-step TIFF chronology, and a compact 128/256-square paired selection. One combined candidate, one causal revision before the final comparison. If a completed valid performance comparison fails, close native. If required conditions remain unavailable after the predeclared bounded observation opportunities, close native unqualified for this release. No repeated 1024 campaigns; final dataset choice and references follow MEASUREMENT.md.

## Finalize even when native loses

In parallel, enumerate main-to-final changes. Keep validated active CPU improvements; move or exclude unselected research infrastructure from installed runtime without erasing evidence or tests. If native loses, remove the N8 auto-dispatch seam, repeated registry work and unqualified native package content from the release path. Preserve existing explicit expert behavior where required, but do not make new maintenance-tree imports reachable to normal users.

Finish with one terminal choice: closed_native_qualified, closed_cpu_only, or blocked_no_safe_merge. Emit source/target SHAs, source-tree and environment hashes, active default route, included/excluded path manifest, exact test commands, raw results and limitations. Native non-selection becomes `not_selected_for_this_release`, not `goal_open`. Keep unresolved 24-real-tile and original-upstream claims explicitly unverified. Rehearse the selected merge in a disposable detached worktree and verify final installed behavior; do not change main or publish. The user's subsequent merge decision should require no new architecture choice.
