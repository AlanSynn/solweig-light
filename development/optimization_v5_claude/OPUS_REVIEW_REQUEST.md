# Request for a separate authenticated Opus Claude Code session

Review one immutable SOLWEIG-light optimization packet as an independent numerical reviewer. This session must actually use Anthropic Opus on a verified route; do not use a GLM-remapped `opus` alias and claim Opus. Do not call Astra/Codex/OpenAI.

The coordinator supplies task ID, exact base and candidate SHAs, absolute read-only checkout/evidence paths, owned-file diff, proof record, actual-kernel and small chronological comparisons, and unresolved questions. Read `optimization_v5_claude/DESIGN_AUTHORITY.md` and only the relevant blueprint/dossier. Inspect the implementation, not just the proof narrative.

Check typed expression order, rounding, existing FMA/profile, mask/raw/NaN/signed-zero behavior, first-step and padding history, global bush conditions, water mutation, all carried state, neighbor ownership, cache keys/lifetimes and public artifacts. A real-number identity is insufficient. Propose the smallest counterexample for any gap. Inherited synthetic algebra is not an actual-kernel result.

Return accept/reject/blocked for this immutable patch, with evidence and residual scope. Reuse valid executed evidence; do not rerun a full suite or initiate large tests. You are not authorized to change scientific policy/tolerance, weaken durability, alter workload, merge main or rewrite the architecture. Ordinary implementation corrections can be requested from its owner without escalating to this chat. If a genuine policy exception is needed, write the requested change and in-scope alternatives in the terminal result.

No repeated progress messages are needed. Finish with a concise structured result; full findings may be attached as a separate evidence document by the coordinator.
