# Optimization branch handover (template, not evidence)

- Branch / base / tip / runtime tree / wheel identities: record.
- Selected commits and independent reviews: record.
- Small verification: list executed and reused tests with dependency keys.
- Final 1024 batch: not_run / passed_once / failed / blocked; raw outcome and checked scope.
- Large numerical reference coverage: record, including gaps.
- Validation and hosted CI cost: record; unknown is not zero.
- Existing scientific exceptions and deferred release gates: list without relabeling.
- Proposed CI-only changes and current required checks: record.
- User changes / conflict risk / then-current main: record.
- Reproduction and later-merge small revalidation commands: record.
- Current disposition: branch remains unmerged; no automatic push/PR/release.
