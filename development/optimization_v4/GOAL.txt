Optimize SOLWEIG-light using optimization_v4/PLAN.md, TASKS_OPTIMIZATION.yaml and VALIDATION_POLICY.md. Read applicable AGENTS once; workers read only assigned dossiers. This replaces v3's costly validation cadence, not numerical/model/API contracts. Reuse valid P0-P6 evidence. Model ancestry: upstream 0d7fe742abeeddd890dd58fc76ed7f78bd47faec; inspected checkpoint: 14e888760727583ef782a4dc0e7a5c7c6e6ff9d1. Record actual HEAD; preserve user work.

Before editing, create an isolated local perf/lean-cpu-v4 branch/worktree from current HEAD, with a unique suffix if needed. Workers use separate owned branches. Integrate reviewed changes into this optimization branch only. Do not push, open a PR, merge main, auto-merge, publish, force-push or overwrite user edits. Deliver a tested branch for the user to merge later.

As Astra, own initial selection, genuine semantic decisions and final review. Delegate bounded implementation, differential debugging, fixtures, runtime work and reports to supported Luna Max or alternatives; record model/effort. Use lower effort for mechanical work. At most four sessions, no recursive agents. One integrator owns shared engine/pipeline/API files; one evidence owner controls benchmarks. Small task packets and <=350-word terminal summaries. No progress polling, transcript replay, duplicate lead implementation or intermediate user approval. Use completion events/blocking waits; workers implement/test/repair autonomously.

Develop small: L0 tiny proof/guard cases; L1 actual-kernel comparisons normally <=128 per side; L2 real 64-128-square TIFF pipeline with full 24/48-step chronology, relevant 153-patch physics, intermediate radiation and all carried state. Short tests do not replace full small chronology. L3 only shortlisted 128/256-square timings and 2-4 small tiles; at most two variants and four chosen resource tuples. A 512 probe needs a specific scaling reason. No routine 1024 runs per edit, family, worker or CI push.

Run affected tests after coherent changes, not every edit/commit. Reuse only matching source/transitive-dependency/test/fixture/reference/environment/profile evidence. Record invalidation; uncertain dependencies require small reruns. Reuse valid JIT/environments; build a wheel for the stable combined candidate, not each experiment. Preserve failures and unchanged fallbacks. No blanket full-suite or large-oracle reruns.

Select the smallest portfolio: exact ray absorption/suffix bounds, ordered radiation/partial evaluation/decode fusion, GVF preparation/prefix/fusion, memory-aware workers. Keep all seven workflows, Torch-free core, API/CLI/artifacts, typed order, first-step/bush/wall/border/water behavior and state. No reduced final work, fastmath/reassociation, changed math profile, surrogate, tolerance relaxation or weaker durability. Candidate code never creates upstream goldens.

Freeze the winner, inputs, hardware/resources, outputs, checkpoint and primary cache regime. Only then run ONE actual 24 x 1024-square batch, 24 timesteps, 153 patches, targeting <=1800 s including startup/JIT, geometry, required exports and publication. No five-pair campaign or large tuning matrix. Reuse matching references; at most one final baseline capture only if essential missing numerical evidence requires it. Otherwise disclose unavailable comparisons. One run is an observation, not reliable throughput qualification. Small scaling predictions never pass the large goal. Retain failures; allow only one causally fixed final retry, not endless unchanged reruns.

Keep development local to avoid CI. Later PRs retain a small real CPU end-to-end gate and required checks; heavy optional matrices are explicit campaigns. Do not disable global CI or bypass protection. Deferred P7/P8/release gates stay visible. Finish unmerged with source identities, executed/reused/deferred checks, timings, memory, costs, blockers and merge-review instructions.
