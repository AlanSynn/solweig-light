# Astra execution request: optimize quickly on a separate branch

Implement the evidence-selected SOLWEIG-light CPU optimizations in this repository. Use `optimization_v4/PLAN.md`, `TASKS_OPTIMIZATION.yaml` and `VALIDATION_POLICY.md`. Read applicable repository AGENTS once, then only relevant dossier sections. This request updates v3's execution/validation cadence: develop on small fixtures, validate 1024 scale only at the end, minimize hosted CI, and leave a separate branch for later merging. It does not authorize numerical shortcuts.

## Branch and authority

Record actual HEAD, user changes, current numerical/math profile and valid existing evidence. Reuse completed P0-P6 work; never reset to the historical inspected checkpoint `14e888760727583ef782a4dc0e7a5c7c6e6ff9d1`. Model/API ancestry remains upstream `0d7fe742abeeddd890dd58fc76ed7f78bd47faec`.

Before production edits create an isolated local integration branch/worktree `perf/lean-cpu-v4`, or a unique safe suffix. Use worker branches/worktrees from a recorded common tip. Preserve staged/untracked/user work and do not auto-stash/reset it. Only reviewed worker commits enter the optimization branch. Do not push, open a PR, merge main, enable auto-merge, publish or force-push. The requested endpoint is a tested branch and merge-review packet; the user will request the merge later.

Proceed autonomously through safe local implementation, execution, inspection, fixing and retesting. Do not ask for intermediate approval. Missing real target data/hardware blocks only the target claim, not available small-scale implementation.

## Delegate work, not repeated supervision

Act as Astra for initial portfolio choice, genuinely cross-cutting semantic decisions and final review. Delegate bounded implementation, actual-kernel differentials, fixtures, counters, runtime plumbing and reports to Luna Max or a supported equivalent. Verify actual available model/effort and record the selection; use lower effort for mechanical work. Do not invent subagent execution or assume routing is cheaper.

At most four sessions initially, no recursive agents, one writer per scope. Ray, radiation, GVF and runtime work can proceed independently. A single integrator owns engine/pipeline/API dispatch and shared metadata. A distinct reviewer checks proof and evidence before integration. Give each worker one <=1200-word packet, its dossier, owned files and `maximum_tier=L2`; terminal summary <=350 words with artifacts by path.

No worker polling, transcript replay, duplicated lead implementation or approval-at-each-step. Use actual completion events or supported blocking waits. This removes supervision overhead, not mathematical review or targeted tests. If no delegation tools exist, use the same scopes serially and disclose it.

## Small-first validation, with no expensive loop

L0: tiny arithmetic/state/guard checks. L1: real untouched-baseline kernel comparisons, normally 16-128 pixels per side. L2: genuine 64-128-square TIFF pipeline with 24/48 chronological timesteps, all relevant 153-patch radiation, masks, intermediate fields, carried state and restart checks. Use a supported existing fixture and limited 256-square exceptions when a structural constraint requires it. Short 2-6-step tests do not replace small full chronology.

L3: one central lab screens shortlists at 128/256 squares, at most two variants per family and four chosen resource tuples. Use a predeclared small paired design; extra pairs only for a close decision, not every edit. A 512 probe requires a specific scaling question. Do not execute 1024 cases during ordinary development, per worker, per family, per commit or in PR CI.

Run affected checks after coherent behavioral changes. Do not rebuild wheels, recreate environments, repeat the full suite or hash large data on every edit. Reuse valid evidence only when source/transitive dependencies, tests, reference/fixture, environment/compiler/math profile and relevant runtime settings match. Record reuse/invalidation. Unknown dependencies trigger conservative *small* reruns. Build an installed wheel once for a stable combined candidate, or when packaging changes require it.

Choose a small portfolio from the existing 56 candidates: proven ray absorption/suffix bounds, ordered radiation/partial evaluation/decode fusion, GVF preparation/prefix/postprocessing, and memory-aware persistent workers. Additional exact reuse/certificates are conditional on a small-data census and residual bottleneck. Do not implement all entries or spend time on elaborate rewrites without evidence.

Preserve all seven workflows, Torch-free NumPy/Numba/SciPy core, API/CLI/TIFF/ZIP/NPZ behavior, original logical domains and complete final workload. Keep fastmath=False, original typed order, existing explicit FMA, first-step/bush/wall/padding/water semantics, raw/categorical visibility and chronological state. No reduced resolution/patches/timesteps/ray support, changed math profile, surrogate, relaxed tolerances or weaker durability. Unsupported fast domains use unchanged fallbacks. Never generate upstream goldens from candidate code.

## Final scale and claims

After small tests, independent review and source/wheel freeze, the evidence owner runs one actual 24 x 1024-square batch with 24 steps and 153 patches, under frozen CPU/RAM/storage, inputs, outputs, checkpoint policy and one primary cache regime. Target <=1800 s; 1200-1380 s is a planning margin, not measured evidence. First-use timing includes initialization/JIT, geometry, required exports, runtime validation/checkpoint and final publication. Use the first tile inside this timed batch as the large preflight, not a separate hidden warmup.

Default full candidate trials: one. Default new full-baseline trials: zero. Reuse valid matching references. If essential final numerical references are absent, allow at most one separately identified frozen-baseline capture with its reason recorded before candidate evaluation; otherwise disclose large numerical parity as unverified. Do not rerun original/default/tuned large matrices solely to claim a speedup. No 5-pair 24-tile campaign, 1024 parameter sweep or automatic secondary large regimes.

Use one exclusive benchmark lease and retain raw times, peak memory, failures, watchdog limits and validation costs. No local builds/tests from other agents during timing. Small-data extrapolation and work/span/bandwidth lower bounds do not pass the real target. Do not multiply overlapping work/thread/worker gains.

On a failed final run retain it, minimize the cause to small fixtures, repair and retest. At most one additional candidate full-batch attempt is preauthorized after a real causal fix/new freeze. No unchanged retries to seek a faster outcome; no indefinite expensive campaign. Deliver the best verified branch with unmet gates if the budget or capability is exhausted.

## CI and stopping point

Default development has no pushes or PR, so do not create hosted CI overhead. If later authorized to prepare/push a PR, use the existing small real CPU end-to-end gate and required checks on one canonical environment; expensive optional matrices remain explicit campaigns. Do not globally disable Actions or bypass branch protections. Do not assume `[skip ci]`, a draft PR or a branch-only dispatch workflow guarantees suppression/availability. Follow `BRANCH_AND_CI.md`.

A single full run is `target_demonstrated_once`, not robust throughput qualification. Keep old P7/P8/Section 13 release requirements and approved scientific failures visible as passed, failed, deferred or blocked according to evidence. Do not run the whole historical release matrix just to finish this optimization branch, and do not declare that release complete.

Final handover: branch/base/tip/source/wheel identities, reviewed changes, executed/reused/deferred test inventory, small timings, actual final result or blocker, raw artifacts, validation/CI cost, reproducible commands and later-merge instructions. Stop with the optimization branch unmerged. No first-prototype-only completion and no promise of unowned background work.
