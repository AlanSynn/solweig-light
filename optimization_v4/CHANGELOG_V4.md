# v4 operational delta

- Retained 56 mathematical/system strategy entries and quarantines; no numerical tolerance or physics change.
- Replaced routine large discovery/calibration with L0-L3 small tests and a single final-scale L4 campaign.
- Replaced proposed five full-batch pairs with one final candidate run, zero new large baselines by default, at most one justified final reference capture and one causal-fix retry.
- Made full historical/platform/release matrices explicit later scope, without marking them passed or bypassing required merge checks.
- Added transitive-identity-based validation reuse; no full-suite/rebuild/re-hash loop after every edit.
- Added mandatory separate local integration branch/worktree, owned worker branches and no automatic main merge/push/PR.
- Added local-first CI minimization with small real end-to-end coverage and protected-check safeguards.
- Added validation ledger, cost caps, final protocol and merge-review templates.
- Kept completion-driven Luna/alternative delegation, independent review and a single benchmark lease.
- Historical v3 analysis logs are preserved under evidence/inherited_v3; new packet checks do not execute SOLWEIG.
