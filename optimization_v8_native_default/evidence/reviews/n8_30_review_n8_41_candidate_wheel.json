{
  "schema": "sw8-n8-30-review-v1",
  "review_of": "N8-41 candidate native wheel (uncommitted diff, 5 modified + 5 new paths) against optimization_v8_native_default/evidence/wheels/n8_41_candidate_wheel_draft.json incl. the integrator_adjudication block; author n8-41-wheel; this review independent",
  "reviewer": "n8-rev-n8-41 (independent; previously reviewed N8-41 vendoring, n841-3 install_assets, n8-43 freeze, N8-40b seam -- not this diff)",
  "review_utc": "2026-09-23T04:49Z",
  "worktree": "/Users/alansynn/Workspace/solweig-v8-native @ HEAD a19e22db557d7755191455a60c4dcda9b757d93d, branch perf/native-optimization; diff UNCOMMITTED",
  "working_tree_at_review_start": "git status --porcelain shows exactly the module_map: M pyproject.toml, M src/solweig_light/_native_dispatch/__init__.py, M src/solweig_light/_native_dispatch/installed_loader.py, M tests/optimization_v8/installed/conftest.py, M tests/optimization_v8/policy/test_legacy_env_parity.py; ?? experiments/optimization_v8/packaging/assemble_native_wheel.py, ?? optimization_v8_native_default/evidence/wheels/, ?? setup.py, ?? src/solweig_light/_native_dispatch/build_native.py, ?? tests/optimization_v8/installed/test_native_wheel_gates.py. The staged generation dylib is gitignored (git check-ignore rc=0), explaining its absence. installed_gates.json clean = HEAD version (focus 7 satisfied at start).",
  "verdict": "APPROVE",
  "blocking_defects": [],
  "required_repairs": [],
  "d1_independent_conclusion": "CONFIRM (I agree with the integrator's ACCEPT; I was asked to verify, not re-adjudicate, and I verified). (a) ENV READS: exactly two literal reads in the vendored driver -- 'PATH' at src/solweig_light/_native_dispatch/build_native.py:128 and 'SOLWEIG_LIGHT_ISPC' at :169 (inside discover_ispc). No module-level env reads (all reads are inside function bodies; module level is imports + defs + constants only). (b) RUNTIME UNREACHABILITY: the installed loader (installed_loader.py) touches ONLY constants -- ABI_VERSION (:145), WRAPPER_ABI_LAYOUT_VERSION (:147), PACKAGE_NAME (:149), MATH_PROFILE_ID (:151) -- and driver.verify_generation (:463). verify_generation (build_native.py:577-648) is JSON parse + validate_manifest + generation_name + _sha256_file hashing + macho_structure_error (pure byte-structure walk, :311) + fma_audit (pure regex on .s text) -- NO subprocess, NO os.environ, NO shutil.which. grep across src/ finds ZERO callers of run_tool/discover_ispc/macho_facts/native_build_impl/_build_env/source_fallback_manifest/atomic_publish outside build_native.py itself (they serve native_build_impl/main, the build pipeline). native_handle dlopens by absolute path (ctypes). lw_default_policy never calls any of them. EXECUTED corroboration: the installed-origin probe ran attempt_load() in a fresh venv with child env {HOME, PATH=/usr/bin:/bin, LANG} and NO backend variables -> status loaded; the selection probe resolved row A in the same env. (c) SELF-VALIDATION MECHANICS: dx_snapshot._env_vars_read_from_tree (tests/optimization_v8/dx/dx_snapshot.py:320-350) is AST-based, records literal os.environ.get/getenv/environ[...] reads (plus SOLWEIG*-prefixed name constants), and returns SORTED per-file lists -- so the vendored file scans to exactly ['PATH', 'SOLWEIG_LIGHT_ISPC'] (the pinned list is the sorted form; read-order changes in the file cannot false-fail). Failure modes verified mechanically: a second literal read, a changed read, a renamed/removed file, or a moved key each makes the pop yield a different value or None -> the assert fails the gate. The frozen baseline contains ZERO occurrences of build_native or SOLWEIG_LIGHT_ISPC (grep -c = 0), so the single pop is necessary and sufficient for the equality. The gate test is green in my runs. (d) NUANCE (note N1): the scanner is blind to DYNAMIC reads -- _build_env's os.environ.get(key) loop forwarding SDKROOT/DEVELOPER_DIR/MACOSX_DEPLOYMENT_TARGET (:130-134) is unrecorded. That is pre-existing scanner semantics shared by the frozen baseline (consistent freeze), but the exclusion's 'exactly' is exact only over what the scanner can see; the build-time-only property rests on the call graph above, not on the scanner. The call graph is solid, so the runtime freeze is genuinely byte-exact.",
  "d4_conclusion": "ACCURATE, no gate semantics changed. The conftest.py pytest_sessionfinish deferred[0] reword now states: selection-pinned native artifact re-freeze -> N8-50, counted native execution evidence -> N8-42, candidate wheel mechanism ships the staged generation, qualification does not exist, shipped registry stays empty -- all four clauses verified true by this review (test_installed_native_selection_stays_row_a: A/auto-legacy/[absent]/record None with the artifact present AND loaded; the deferred gate's own docstring requires 'automatic native selection, actual native work counted', still unmet). List structure and skip behavior unchanged (pure string reword; diff shows 4 lines -> 6 lines in one list element).",
  "claims": [
    {
      "claim": "1. Byte-identical staging + finally-unstage in the build shim; purity-guard interaction (crashed-build leftover)",
      "status": "PASS",
      "finding": "setup.py:102-118 (_GuardedBuildPy): native request -> _identity(request) runs the FULL gate (verify_for_wheel) BEFORE anything is staged; stage_generation copies with copy2 and PROVES byte-identity member-by-member by sha256 (assemble_native_wheel.py:339-349, mismatch -> rmtree(target) + raise, :350-352); super().run() wrapped in try/finally with unstage_generation (:115-118). unstage_generation (:356-364) rmtree's the generation and rmdir's the emptied native_generated container -- tree returns to pre-build state. CRASHED-BUILD leftover: a hard kill between stage and finally leaves native_generated populated; the next PURE build then fails LOUD in assert_pure_package_tree (:309-320) instead of shipping native content in a py3-none-any wheel -- the guarded case is exactly the recorded one. The staged generation is FLAT (4 files: dylib, .h, .s, manifest.json -- ls verified), so the top-level hash proof covers every member (see N4 for the nesting caveat). stage_generation refuses to stage over an existing target (immutable generations)."
    },
    {
      "claim": "2. Both gate directions actually executed (pip-level loud failure zero wheels; corrupt/renamed rejected; pure wheel ships zero native members)",
      "status": "PASS",
      "finding": "EXECUTED in my installed rerun (22/6). test_native_request_with_unusable_output_fails_the_build: full pip wheel with SOLWEIG_LIGHT_PACKAGE_NATIVE=/tmp/n841-nonexistent-generation asserts returncode != 0, 'not a directory' in output, and ZERO *.whl in the dist dir. test_assembly_gate_rejects_corrupt_and_renamed_generations: byte-flipped dylib (raw[100] ^= 0xFF) -> AssemblyError matching 'hash' (verify_generation's per-file sha256); renamed directory -> 'name mismatch|verification' (content-derived generation_name gate). test_pure_wheel_ships_no_native_content: the session's ordinary built_wheel is py3-none-any and contains ZERO native_generated/.dylib/.so members. Author's manual back-to-back builds (platform-tagged vs py3-none-any, clean src tree) are recorded in the record and consistent with the shim's control flow."
    },
    {
      "claim": "3. Installed-origin probe: fresh venv outside repo, zero repo sys.path entries, sha match vs packaged manifest",
      "status": "PASS",
      "finding": "test_installed_native_loader_loads_the_packaged_generation (EXECUTED green): builds the candidate wheel by ordinary pip, installs into a fresh uv venv with the pinned offline closure, then runs a child python with env {HOME, PATH=/usr/bin:/bin, LANG, NUMBA_CACHE_DIR} (ZERO backend variables) and cwd OUTSIDE the repo. Probe asserts: status 'loaded'; generation == staged generation; generation_dir AND resolved driver both under the venv's site-packages; repo_sys_path == [] and '/src/solweig_light' not in driver_file; handle stamp kernel_sha256/dylib_sha256/build_mode equal the staged manifest identity; ABI_VERSION==1, WRAPPER_ABI_LAYOUT_VERSION=='lw-region-abi-1', PACKAGE_NAME=='solweig-light'. This concurrently proves the VENDORED build_native module functions from site-packages (the loader's verify_generation runs there). COUNTED native execution remains honestly deferred (N8-42), as the record states."
    },
    {
      "claim": "4. Selection invariance: registry_gate tests from 3079d69a untouched and green; shipped registry byte-identical; auto still row A with the artifact present and loaded",
      "status": "PASS",
      "finding": "git diff --stat over tests/optimization_v8/installed/test_installed_noenv_gates.py and test_source_fallback_gates.py is EMPTY (untouched); src/solweig_light/_native_dispatch/qualification_registry.json is not in the diff (byte-identical). test_installed_native_selection_stays_row_a (EXECUTED green, in the NATIVE wheel venv): row='A', mode='auto-legacy', expert=False, record None, registry path under site-packages, reason starts '[absent] no qualification records', '[malformed]' absent. The registry-gate pair inside the installed run is green. Selection is records-blind to packaging: nothing in setup.py/assemble_native_wheel.py touches the registry, and no src file reads SOLWEIG_LIGHT_PACKAGE_NATIVE (only a docstring mention in _native_dispatch/__init__.py)."
    },
    {
      "claim": "5. Vendored driver byte-identity vs experiments source + drift alarm",
      "status": "PASS",
      "finding": "shasum -a 256: src/solweig_light/_native_dispatch/build_native.py == experiments/optimization_v8/packaging/build_native.py == dd2467b3dcf8c46cfef69591339334d5a42d62ab788fa7fdf768757a15604d50 -- byte-identical AND equal to the record's claimed digest. Drift alarm test_vendored_build_driver_matches_the_reviewed_experiments_source byte-compares both files (EXECUTED green). installed_loader._build_native resolution: repo tool home first (in-repo behavior UNCHANGED, sys.modules['build_native'] stays ONE shared module), else the vendored package submodule, else loud ImportError naming BOTH paths; docstring updated, no gate logic touched."
    },
    {
      "claim": "6. setup.py inertness for ordinary pure builds; bdist_wheel delete-then-raise window",
      "status": "PASS",
      "finding": "No request: _NativeAwareDistribution.has_ext_modules() returns False (pure), _GuardedBuildPy runs only assert_pure_package_tree + super().run(), _GuardedBdistWheel returns after super().run(); distclass/cmdclass are the ONLY setup kwargs -- metadata stays in pyproject.toml (diff verified). built_wheel n8-23 gates green unchanged in the installed run; py3-none-any asserted by test 7. Deleting setup.py returns the pre-N8-41 build (nothing stages without the shim). DELETE-THEN-RAISE: _GuardedBdistWheel verifies every recent wheel after bdist_wheel; on ANY failure it unlinks ALL recent wheels BEFORE raising -- within the single pip process nothing survives to be copied/uploaded; a native-requested build can never ship a pure-looking or non-identical wheel. MINOR (N3): the mtime window 'started - 5' can sweep a pre-existing solweig_light-*.whl built <5 s earlier into the deletion set on a failing native build -- maintainer-tree only, fail-loud direction, deletion not shipping."
    },
    {
      "claim": "7. installed_gates.json: committed-state file is the HEAD version; my reruns regenerate it (expected, not author drift)",
      "status": "PASS",
      "finding": "At review start the file was clean == HEAD (verified via git status; HEAD version contains NO native_wheel keys -- grep of git show HEAD is empty, consistent with the coordinator's restore-before-commit statement). My installed rerun regenerated it: the diff is ADDITIVE gate content -- new GATE_RECORD keys native_wheel, native_wheel_candidate_identity, native_wheel_gate, native_wheel_venv (plus native_installed_origins) all with 'status': 'passed'; NO api/cli/surface/registry status lines removed (the only '-' lines are the known run-metadata churn: pytest tmp-dir indices and memory-budget figures inside captured ResourceAdmissionError strings). This is the disclosed suite-written class; the integrator should restore before commit. NOT author drift."
    }
  ],
  "suite_reruns": {
    "load_discipline": "1-min loadavg before the installed run 10.81 (00:41), before the full tree 4.29/4.35 -- all >= 2.5; no run entered a low-load quiet window (load was high throughout); no timed/benchmark work performed",
    "installed": "22 passed / 6 skipped in 166.36s -- matches the author's 22/6 and the coordinator's 163.99s rerun; skip decomposition unchanged (4 deferred + 2 host-memory-pressure), no new skips",
    "full_tree": "tests/optimization_v8/ ONE invocation: 1981 passed / 6 skipped / 0 failed in 213.23s = 1972 baseline + exactly the 9 new tests; 4 warnings = the pre-existing pair class (region fork DeprecationWarnings + native cast RuntimeWarning)"
  },
  "notes": [
    "N1 (D1 nuance, no action): the dx scanner records only LITERAL env reads; _build_env's dynamic forwarding of SDKROOT/DEVELOPER_DIR/MACOSX_DEPLOYMENT_TARGET (build_native.py:130-134) is invisible to it, in both the frozen baseline and this gate. The exclusion's exactness is scanner-scoped; the load-bearing build-time-only claim rests on the call graph, which I verified end-to-end (see d1_independent_conclusion).",
    "N2 (doc-only residue, recommend reword at the N8-50 re-freeze): test_deferred_gates.py's module docstring still pins '@ 16cdc56c ... no packaged native artifact' and the [native-wheel-deferred-N8-41] skip reason's first sentence ('no packaged native artifact exists on this branch yet') is now literally false in-suite (the new tests build one). The SKIP itself remains correct -- that gate's requirement is automatic selection + counted native execution, still unmet. The integrator's conftest-only reword scope was conscious; semantics unaffected.",
    "N3 (minor, maintainer-tree only): the bdist_wheel post-verify deletion set uses an mtime window (started - 5 s) that could delete an unrelated pre-existing solweig_light wheel built seconds earlier when a native build fails verification. Fail-loud direction, nothing mislabeled can ship; acceptable as-is.",
    "N4 (forward-looking): stage_generation's byte-identity proof hashes top-level FILES only; the current generation is flat (4 files) so coverage is exact. If a future generation nests subdirectories, extend the proof (or rely on verify_built_wheel + the installed probe, which hash the dylib and manifest end-to-end).",
    "N5 (expected churn): my installed rerun regenerated installed_gates.json exactly as the coordinator predicted (additive native_wheel keys + run-metadata churn); restore before commit.",
    "N6 (positive): the record labels the candidate wheel sha256 'run_scoped' -- wheels embed timestamps so the digest differs per build; identity is pinned through the CONTENT (dylib/manifest sha256 vs the staged manifest), which is the right invariant. D2 (scan in the assembly gate, not build_native.py) and D3 (bare-basename install name, no post-hoc install_name_tool mutation) are both sound: D2 preserves the vendoring byte-identity that makes the drift alarm exact and keeps subprocess tools out of the runtime-reused verify path; D3 avoids breaking content-derived identity, the loader dlopens by absolute path, and the gate accepts the canonical @rpath form for the N8-50 re-freeze."
  ],
  "conclusion": "The candidate wheel does what the record claims: ordinary pip stays the pure fallback (guarded, never pretending); the native request runs the full N8-20 verification plus linked-image/install-name gates BEFORE staging, stages byte-identically, unstages in finally, and deletes-then-fails on any wheel violation; the installed loader loads the PACKAGED generation from site-packages with zero repo reach; shipped selection stays auto -> row A with the artifact present and loaded. D1's runtime-freeze claim is independently CONFIRMED (call graph + executed minimal-env probe); D4's reword is accurate with no semantic change. All reruns match the author's numbers. Verdict APPROVE."
}
