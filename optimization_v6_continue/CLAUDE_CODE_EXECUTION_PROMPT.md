# Execute the same-branch throughput continuation

You are the GLM-5.3 coordinator in an existing Claude Code project. The user authorizes local implementation and independent Opus/GLM assistance with no artificial token or total-agent cap. You must execute the implementation, verification and repairs, not return another high-level plan and stop. High-level decisions have been supplied by this packet; ordinary proof refinement and implementation decisions remain inside Claude Code. There is no required Astra/Codex invocation.

## 1. Continuation and scope

Locate the worktree already on `perf/claude-glm53-cpu-v5`. Continue there. Do not use old v5 setup instructions to create a new optimization branch. Record HEAD, source hashes and protected dirty paths; do not reset to e7a2d6ec, stash, rebase or touch main. Create only detached worker worktrees at a verified immutable commit when isolation is needed. Only the integrator commits to the named integration branch. No push, PR, CI trigger, merge to main, release, credential modification or destructive cleanup is authorized.

Read the short authority, plan, validation policy, routing/delegation rules and task DAG once. Preserve the source-bound v5 results and accepted changes. Do not copy raw evidence archives into every worker context. Use dossier paths and concise task packets.

## 2. Evidence repair before selecting optimizations

Append, rather than rewrite, a scope correction: 598.9 seconds demonstrates two spatial 1024-square scenes with 24 chronological steps each. The original goal is 24 distinct target spatial tiles, not 24 bands. The old timed field surrounds `thermal_comfort`; do not rely on a narrative saying preprocessing was excluded when the timer encloses it. Record exact timing boundaries.

Run small separate-process probes and inspect public dispatch. Store requested/configured/effective Numba masks, actual parallel routes, admitted workers and environment identity. Prior in-process RuntimeOptions sweeps are diagnostic path comparisons, not established strong-scaling curves. Reuse the existing original and candidate reference environments; do not restart P0-P6.

## 3. Initial implementation portfolio

Independently prepare these tracks:

A. Geometry reuse: prove the standalone and pipeline numerical producers equivalent on supported normalized input. Both start from `geometry_identity`, but standalone appends construction and implementation fields while pipeline does not. A full cold run can construct the same geometry twice. Use a single versioned numerical recipe and complete dependency key, separate from export provenance. Retain standalone export existence/validation/overwrite semantics, source change guards, legacy_cache_policy, all 19 geometry fields and original result mapping. Do not relabel unverified old caches or bypass exports. Count actual kernel invocations on small real TIFFs.

B. Demand-specific radiation: add private pipeline-only fast paths for anisotropic Lside (four original Lup*0.5 operations), reduced cylinder longwave diagnostics, and cylinder shortwave block scratch. Preserve every term that enters TMRT/state/future reads. Keep the complete public/trace path; never populate omitted diagnostics with fake values. Treat warning/exception/alias semantics as proof obligations, not an excuse to skip validation.

C. Runtime/phase concurrency: reuse the existing persistent pool. First parallelize immutable native geometry computation while ordered publication remains compatible; preserve the geometry-before-simulation barrier. Expand export concurrency only if measured and failure/publication semantics are preserved. Derive per-stage memory with encoded modes, raw fallback, parent/native reserve and mapped lifetime; don't replace the old estimate with the observed 1.1 GiB. Select worker/thread/block settings on small same-resource batches.

D. GVF: existing G02 caches converted snapshots but still evaluates expressions per direction; preserve first/later water state and aliases while hoisting actual invariant expressions. Compile the remaining ordered block postprocess with explicit original promotions. Prefix/repeated-add and static nosh reuse are conditional follow-ons.

E. Decoder/storage: batch known immutable-channel preparation and decode calls while retaining the current accepted reducers. Do not resurrect the slower all-in-one P01 design. If export costs dominate, combine full CRC/schema/value verification into a bounded read pass, preserving error precedence by a slow diagnostic fallback when necessary.

Use the extended register only for residual bottlenecks. Do not concurrently implement every idea or multiply overlapping estimated gains. Each patch is narrow, independently reviewed and measured at the appropriate scale before integration.

## 4. Delegation and execution

Unlimited inference is permission to use useful independent workers, not permission to overrun machine resources. Use true Opus for difficult proofs/implementation/review when an authenticated route exists; record absence honestly and continue with independent GLM reviewers if not. Do not build a routing platform. Establish provider separation without dumping secrets or rewriting global config.

Each task names immutable base, owned files, private interface, guard/proof obligations, permitted validation tier, resource reservation and terminal artifacts. Native completion events or supported blocking waits replace status polling. No recursive agent explosion. Same-file alternatives stay in detached worktrees. The integrator alone handles shared API/pipeline/engine/schema edits and rechecks interactions.

Reviews are independent and completion-triggered, not repeated full test runs. Workers repair their own ordinary bugs without user approval. Read-only inference may continue during a benchmark only when it causes no workload on the measured host; stop all other builds/tests/profilers there.

## 5. Verification and completion

Develop with L0/L1 and small genuine 64/128-square TIFF-to-TIFF chronology, not numeric mocks. Use 128/256-square small distinct-tile batches for current source/configuration comparisons. No per-worker/per-commit 1024 admission, automatic hosted CI or historical matrix. Reuse unchanged checks by dependency/source/profile identity, never just by filename.

Freeze a new protocol and source/wheel before a final campaign of 24 actual spatial tiles, each with 24 steps and 153 patches. Keep resolution, context, forcing, physical components, outputs and checkpoint policy equal. Final default is one candidate campaign and zero new large baselines; one justified missing-reference capture or explicit unverified scope follows the validation policy. One causal final retry is allowed, not an unbounded benchmark loop. If actual target data is absent, do not fabricate completion from repeated two-scene fixtures.

Report measured throughput against a verified same-resource baseline, separately from hardware-budget increases, hypothetical models and historical single-case ratios. Target <=1800 seconds is an observed criterion, not a guarantee that mathematics alone can establish. A deadline abort, OOM, failed numeric field or unavailable source remains visible. The two old 1024 outputs cannot stand in for complete 24-tile numerical reference coverage.

Finish with reviewed commits on the SAME branch; concise source/result/command manifest, exact scope of tests and speed claims, unavailable gates and next dependencies. Do not stop at a scaffold/import/unit-test prototype. Do not prolong work after a sufficient verified portfolio merely to implement the catalog.
