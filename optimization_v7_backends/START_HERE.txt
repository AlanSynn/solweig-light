Continue SOLWEIG-light on the EXISTING branch perf/cpu-optimization using Claude Code and the current GLM-5.3 session. Implement the CPU backend experiments in optimization_v7_backends; do not stop after returning a plan. No Astra/Codex calls are required.

Read applicable repo instructions, then optimization_v7_backends/PLAN.md, DESIGN_AUTHORITY.md, KERNEL_CONTRACT.md, VALIDATION_POLICY.md, BENCHMARK_PROTOCOL.md and TASKS_CLAUDE.yaml once. Preserve newer HEAD, dirty files, v6 improvements, current numerical/reference policies and slim CI. Only the integrator commits on this branch; detached worker worktrees at explicit immutable bases are allowed. No new branch, reset, push/PR/merge, broad CI, credentials changes or destructive cleanup.

Goal: improve measured CPU throughput without changing all seven APIs, CLI/TIFF/legacy artifacts, physical resolution/context, 153 patches, timesteps, vegetation, ray support, radiation or chronological state. Keep Torch/CUDA out of the own-met core. The actual target is 24 spatial 1024-square tiles, EACH 24 timesteps, <=1800s under fixed resources; the historic 598.9s was two scenes, not 24 tiles.

First island: the reached cylinder_longwave._longwave_primary reducer. Capture immutable real arguments, native scalar/array types and seven output columns from the untouched accepted baseline. Preserve its two ordered patch sweeps and reflection dependency. Keep original SLEEF/classifier/visibility decoding outside the first port.

Compare A=current accepted Numba, B=same new layout/scheduling in Numba, C=matching foreign CPU backend. Include packing, validation, conversion, JIT, synchronization and host materialization. Do not call layout gains framework gains. B is the preferred outcome if it matches/beats C.

Delegate independent prototypes to ISPC (or Highway), Dr.Jit LLVM, and PoCL CPU through PyOpenCL when available without privileged setup. MLX CPU/Halide is conditional on a measured pointwise residual. Do not install every surveyed framework. Keep previously rejected fused radiation and prepared decoder OFF.

Preserve the typed expression graph: no new contraction/FMA, reordered patch reduction, reciprocal approximation or silent float64 downcast. Check exact finite bits, signed zero, nonfinite masks, all seven outputs, state and guard failures. Explicitly select CPU: Dr.Jit LLVM/non-AD with FastMath disabled; OpenCL CPU with strict contraction/math settings and qualified capabilities. Numerical failure in an admitted domain is not silently hidden by fallback. Unsupported inputs retain the reference path before side effects.

Use actual Opus for difficult implementation/proof/review when authenticated; verify routing because opus aliases may map to GLM. Otherwise label independent GLM review honestly. No artificial inference/agent cap, but bound CPU/RAM/build jobs and give benchmarks one exclusive owner. No progress polling or repeated transcript reads. Workers implement, test, inspect and repair autonomously; separate reviewers assess immutable patches. Shared dispatch/identity/packaging files have one integrator.

Develop with tiny kernel tests, actual 64/128-square TIFF chronology of 24/48 steps, then 128/256-square small multi-tile timings. Query effective native pools in independent processes. Reuse unchanged evidence. No per-worker/commit 1024 tests or full suite. Freeze before one final actual 24-tile campaign; zero new large baselines by default and at most one causal final retry. Missing corpus/reference means unverified, never replaced by duplicated scenes.

Integrate only exact, end-to-end beneficial changes as optional CPU backends; retain Numba default/core fallback, bounded buffers and correct compiler/device/profile identities. Report measured A/B/C results, qualified scope, failures, commands and unverified gates. No stubs or invented gains. If no new backend wins, retain the best verified Numba path and finish with explicit rejection evidence.
