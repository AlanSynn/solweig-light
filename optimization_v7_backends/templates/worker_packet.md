# One bounded task

Task ID; actual model/provider/effort attribution:
Immutable base SHA and owned worktree/paths:
Shared files NOT owned:
Dossier and contract sections:
Concrete code/test deliverable (not an import stub):
Input/reference fixtures and admission domain:
Required exact outputs, mutations, warnings and errors:
Permitted validation tier and CPU/RAM/build reservation:
Lab lease before timing:
Terminal code/diff/proof/log/result paths:

Implement -> execute -> inspect -> repair -> focused tests. Emit one terminal
summary or genuine blocker; no manager polling. Request an independent review
with the immutable final delta. Do not commit integration branch from a worker.
