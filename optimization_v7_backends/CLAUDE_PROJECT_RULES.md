# SOLWEIG optional CPU backend task

Continue only on `perf/cpu-optimization`; protect newer HEAD and dirty files. One integrator commits. Detached worker worktrees at explicit immutable bases are allowed. No auto push/PR/main merge, hosted CI expansion, global dependency/credential changes, or destructive cleanup.

Read `optimization_v7_backends/PLAN.md` and `TASKS_CLAUDE.yaml` once at kickoff. Workers read only their dossier plus `KERNEL_CONTRACT.md`/the relevant validation rules. Do not auto-import the entire archive or historical reports.

Default core stays NumPy/Numba/SciPy, Torch/CUDA-free. Add an optional backend only after exact current-profile admission and a measured end-to-end advantage over A (accepted Numba) AND B (same new layout in Numba). First island: accepted longwave primary's two ordered sweeps and seven outputs. Keep original decoder/SLEEF preparation outside. No new FMA/reassociation or silent casts; unsupported profiles fall back before effects, admitted mismatches fail visibly.

Delegate useful independent implementation and review to actual GLM/Opus routes with no artificial inference cap. Do not claim a GLM-mapped opus alias is Anthropic. No Astra call or manager status polling. Local CPU/RAM/queue budgets remain bounded; timed host is exclusive. Do not run concurrent Python tile calls through module-global demand state.

Small actual-kernel and 64/128² full-chronology tests drive development. 128/256² small batches select candidates. No per-backend/per-commit 1024 tests. Final actual 24-spatial-tile campaign at most once, plus one causal corrective retry under the frozen protocol. Missing corpus/reference means unverified, not an easy-fixture substitute.

Review precedes integration. No public stubs or fake measurements. No framework win is a valid outcome: retain improved Numba or unchanged accepted code and report rejections. Finish implementation, testing and disposition, not another plan-only response.
